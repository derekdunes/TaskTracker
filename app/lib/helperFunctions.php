<?php

if ( ! function_exists('sendMail')){
	
	// function sendMail($title, $content, $to, $from ){

	// 	Mail::send('emails.send', ['title' => $title, 'content' => $content], function ($message)
 //        {

 //            $message->from( $from, 'Lloydant Support');

 //            $message->to($to);

 //        });

	// }
}